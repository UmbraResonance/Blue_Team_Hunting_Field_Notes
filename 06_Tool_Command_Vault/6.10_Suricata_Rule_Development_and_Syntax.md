# 6.10 Suricata Rule Development and Syntax Guidelines

## 1. Engine Execution & Performance Optimization

Suricata's detection engine does not execute rules in the order they are written. Understanding the Multi-Pattern Matcher (MPM) is critical for writing rules that do not cause network latency or packet drops.

### The `fast_pattern` Pre-Filter
The `fast_pattern` modifier is the primary performance safeguard for any complex rule. 

* **Mechanism:** Suricata extracts the longest, most unique string tagged with `fast_pattern` and loads it into the MPM search tree. If this exact string is absent in the packet payload, the engine immediately drops the rule evaluation, bypassing all expensive PCRE and buffer checks.
* **Best Practice:** Always apply `fast_pattern` to a highly unique string sequence. For example, `.php HTTP/1.1\r\nCookie: session=` is an excellent candidate because it defines a strict sequence of URI termination, protocol version, line break, and a specific header, which is extremely rare in benign traffic.
* **Syntax Independence:** The physical placement of `fast_pattern` within the rule string does not dictate its execution order. It acts as an absolute prerequisite for the rule to trigger deeper inspection.

## 2. Payload Navigation: Modifiers vs. Buffers

Navigating the packet payload effectively prevents evasion techniques like whitespace injection and protocol smuggling.

### Absolute Positioning (The "Caliper" Approach)
Used primarily for binary carving, raw TCP/UDP payload inspection, or as a fallback when protocol parsers fail.

| Modifier | Function | Evasion Defense Context |
| :--- | :--- | :--- |
| `offset` | Starting byte for the search. | Finding magic bytes (e.g., `MZ` at `offset:0`). |
| `depth` | Maximum bytes to search from the start. | Restricting searches to specific protocol headers. |
| `distance` | Bytes to skip after the *previous* match. | `distance:0` prevents zero-byte gap evasions (e.g., `POST/login.php` without any spaces). |
| `within` | Maximum search window after the distance. | Ensures the subsequent match occurs within a logical boundary. |

### Sticky Buffers (The "Drawer" Approach)
Relies on Suricata's internal parsers to normalize traffic. This is the modern standard for application-layer protocols (HTTP, DNS, TLS).

* **Mechanism:** Parsers automatically strip evasion attempts (e.g., substituting spaces with tabs `\t`, random carriage returns `\r\n`) and place the normalized data into predefined buffers.
* **Syntax Evolution:**
    * *Legacy (Suffix):* `content:"GET"; http_method;` 
    * *Modern (Prefix):* `http.method; content:"GET";` (Recommended for clarity and efficiency).
* **Advantage:** Drastically reduces false positives and CPU overhead by restricting the search space to a specific protocol field (e.g., targeting `http.user_agent` instead of the entire packet).

## 3. PCRE (Perl Compatible Regular Expressions) Utilization

PCRE should be utilized exclusively as a final validation step after cheaper `content` matches and `fast_pattern` checks have been satisfied.

### Syntax and Modifiers
* **Structure:** Must be enclosed in forward slashes `/"..."/`. Modifiers are appended after the closing slash (e.g., `/CRi`). The order of appended modifiers (e.g., `/iRC` vs. `/CRi`) does not alter execution logic.
* **Key Suricata Modifiers:**
    * `R` (Relative): Initiates the regex match exactly where the previous `content` match terminated.
    * `U` (URI): Forces the regex execution inside the `http_uri` buffer. Note: `RU` is a macro for Relative + URI.
    * `i` (Case-Insensitive): Ignores case constraints.

### Advanced Threat Hunting Patterns
1.  **Negative Matching (Allowlisting):** Instead of building an exhaustive blocklist for command injection characters (which is prone to bypasses), use `!` to invert the PCRE and define an allowlist of legitimate characters.
    * *Example:* `pcre: !"/^\$?[\sa-z\\_0-9.-]*(\&|$)/iRP"` triggers only when a character *outside* the defined safe set (e.g., `;`, `|`, `` ` ``) is observed.
2.  **Non-Capturing Groups `(?:...)`:**
    Always use `?:` inside grouping parentheses unless the matched value is required for a subsequent rule operation. This prevents the engine from allocating memory to store the matched string, saving critical CPU cycles during high-throughput inspection.

## 4. Real-World C2 Detection Mechanics

Signatures for modern Command and Control (C2) frameworks require a fusion of string matching, behavioral analysis, and anomaly detection.

* **PowerShell Empire (Anomaly Identification):**
    Focus on the *absence* of standard behaviors combined with specific artifact structures. Detection logic includes matching pseudo-legitimate URIs (e.g., `login/process.php`), validating Base64 encoding structures within the `session` cookie via PCRE, and using negative content matches (`!"Referer"`, `!"Cache"`) to flag the unnatural absence of standard browser headers.
* **Covenant (Statistical Filtering):**
    Utilize rhythmic behavioral analysis for beaconing detection. Applying `detection_filter: track by_src, count 4, seconds 10` shifts the detection paradigm from a single string match to a frequency threshold, identifying automated check-ins while eliminating accidental human-generated false positives.
* **Sliver (Structural Contrast):**
    Identify frameworks that attempt to blend in by contrasting "legitimate-sounding" directories with anomalous parameters. Detection relies on complex PCRE to map dynamic URI paths (e.g., `/api/v1/login.php`) that conclude with excessively short, randomized query parameters (e.g., `?a=123`) used for session tracking.